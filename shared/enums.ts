export enum CurrentActionUser {
  IDLE = "IDLE",
  IN_ROOM = "IN_ROOM",
  PLAYING = "PLAYING",
}

export enum RolesUser {
  User = "user",
  Admin = "admin",
  SuperAdmin = "super_admin",
}

export enum ManagePlayerReadiness {
  READY = "READY",
  NOT_READY = "NOT READY",
}
